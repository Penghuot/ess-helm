FROM matrixdotorg/synapse:latest

# Copy your config into the expected location
COPY homeserver.yaml /data/homeserver.yaml

# Let the image’s default entrypoint run Synapse
# (no CMD override needed)
